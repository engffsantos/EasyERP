# ================================
# 🔐 Chaves de segurança
# ================================

SECRET_KEY=super-secret-key         # Chave secreta do Flask (protege sessões, CSRF, etc.)
JWT_SECRET_KEY=jwt-secret-key       # Chave usada para assinar os tokens JWT
JWT_EXPIRES_IN=3600                 # Tempo de expiração do token JWT em segundos (padrão: 1h)

# ================================
# 🗄️ Banco de Dados
# ================================

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/easyerp
                                       # URL de conexão com o PostgreSQL

# ================================
# 📧 Configuração de E-mail (SMTP)
# ================================

EMAIL_HOST=smtp.gmail.com             # Servidor SMTP
EMAIL_PORT=587                        # Porta SMTP (TLS)
EMAIL_USERNAME=seuemail@dominio.com   # Usuário SMTP
EMAIL_PASSWORD=sua_senha              # Senha ou app password
EMAIL_FROM=seuemail@dominio.com       # Remetente padrão dos e-mails

# ================================
# 🗂️ Diretórios e arquivos
# ================================

BACKUP_DIR=backups                    # Diretório onde os backups serão salvos
LOG_FILE=logs/easyerp.log             # Caminho do arquivo de log da aplicação
WEASYPRINT_BASE_URL=http://localhost:5000
                                      # Base URL para geração de PDFs (WeasyPrint)

# ================================
# ⚙️ Ambientes
# ================================

DEBUG=True                            # Ativa o modo debug do Flask
TESTING=False                         # Habilita modo de testes

# ================================
# 🌐 Docker / Infraestrutura
# ================================

POSTGRES_DB=easyerp                   # Nome do banco_
